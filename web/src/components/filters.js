const React = require('react');
const Base = require('./base');

const stores = {
  status: require('../stores/statusStore')
};

class Filters extends Base {
  render() {
    return (
      <div/>
    );
  }
};

module.exports = Filters;
